# TODO


database.js -> refactor ? 

get("user/login", ) dans Login

onMount dans homepage
onMount(async() => {
	const UserLoggedInStatus = async () => {
		...
	}

	update ${user}
})

en fait on peut faire le "fetch" dans load (page.ts)
returnedData -> data.returnedData, acces dans tous les childs
